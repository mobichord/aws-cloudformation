# Resources:
#   MyEC2Instance:
#     Type: AWS::EC2::Instance
#     Properties:
#       ImageId: ami-023e152801ee4846a  # Specify the desired AMI ID
#       InstanceType: t2.micro  # Specify the instance type
#       SubnetId: "	subnet-ff395aa4"  # Specify the subnet ID of your existing subnet
#       SecurityGroupIds:
#         - "sg-09e90b917cdaf237b"  # Specify the security group ID(s) of your existing security group(s)
#       KeyName: oregon-dev  # Specify the key pair for SSH access
#       UserData:
#         Fn::Base64: !Sub |
#           #!/bin/bash -xe
#           yum update -y
#           yum install -y httpd
#           systemctl start httpd
#           systemctl enable httpd
#           echo "<html><body><h1>Hello World!</h1></body></html>" > /var/www/html/index.html
#       Tags:
#         - Key: Name
#           Value: CFTestInstance